# Cloudflare: set every application variable under Settings → Variables and Secrets.
# Use plaintext for browser-safe settings and Secret for credentials listed below.
SITE_URL=https://kuest.com

# Dub affiliate attribution (server-only keys must be Cloudflare Secrets)
AFFILIATE_DATA_API_URL=https://data-api.kuest.com
AFFILIATE_RPC_URL=https://polygon-amoy.drpc.org
AFFILIATE_DEPOSIT_WALLET_FACTORY=0x2CcdC6C5dDcd895aFcCD259F291de9b618A5cA6c
AFFILIATE_KUEST_FEE_RECEIVER=0x645E67CC15DAE4F312dc941fA190c52E7d598c67
DUB_API_KEY=
AFFILIATE_CHAIN_ID=80002
AFFILIATE_CONFIRMATIONS=64
AFFILIATE_TOKEN_DECIMALS=6
AFFILIATE_START_BLOCK=0
AFFILIATE_DRY_RUN=true
AFFILIATE_BATCH_LIMIT=25
AFFILIATE_MAX_ATTEMPTS=8
AFFILIATE_MAX_HISTORY_PAGES=20

# Optional defaults shown in UI
GITHUB_APP_URL=https://github-app.kuest.com
DEFAULT_VERCEL_TEAM_ID=
DEFAULT_VERCEL_REGION=dub1
DEFAULT_SUPABASE_REGION=eu-west-1
KUEST_CHAIN_MODE=amoy
REOWN_APPKIT_PROJECT_ID=
VERCEL_ALLOW_TOKEN_FALLBACK=true

# Kuest API endpoints used for wallet-based key generation
CLOB_URL=https://clob.kuest.com
RELAYER_URL=https://relayer.kuest.com

# Supabase browser credentials
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Protocol pitch deck requests
RESEND_API_KEY=
RESEND_FROM_EMAIL="Kuest <hello@kuest.com>"
PROTOCOL_PITCH_DECK_TO_EMAIL=hello@kuest.com

# Optional: force default prefix used by Vercel Supabase integration metadata.
# Launchpad already retries common values automatically.
VERCEL_SUPABASE_REGION=eu-west-1
VERCEL_SUPABASE_PUBLIC_ENV_VAR_PREFIX=NEXT_PUBLIC_

# Required only when using OAuth mode for Vercel auth.
VERCEL_OAUTH_CLIENT_ID=
VERCEL_OAUTH_CLIENT_SECRET=
VERCEL_TEAM_ID=

# Required only when using Supabase OAuth.
SUPABASE_OAUTH_CLIENT_ID=
SUPABASE_OAUTH_CLIENT_SECRET=
SUPABASE_OAUTH_SCOPES="openid offline_access"
SUPABASE_OAUTH_ORGANIZATION_NAME=

SUPABASE_SERVICE_ROLE_KEY=

# Optional API rate limit tuning (per IP).
# Defaults are intentionally high to avoid blocking normal retries.
RATE_LIMIT_WINDOW_MS=600000
RATE_LIMIT_LAUNCH_MAX=30
RATE_LIMIT_SUPABASE_RESOURCES_MAX=240
RATE_LIMIT_DOMAIN_MAX=90
RATE_LIMIT_DOMAIN_REGISTER_MAX=600
RATE_LIMIT_PROTOCOL_DECK_MAX=12
RATE_LIMIT_REOWN_CONNECTION_MAX=240
RATE_LIMIT_VERCEL_CONNECTION_MAX=240
